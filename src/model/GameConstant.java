package model;

/**
 * Holds shared constants used throughout the game.
 * Keeping these values in one place makes the code easier to maintain.
 */
public class GameConstant {

    /** Size of each tile in pixels. */
    public static final int TILE_SIZE = 32;

    /** Path to the player sprite inside the classpath. */
    public static final String PLAYER_SPRITE = "/images/player.png";

    /** Path to the zombie sprite inside the classpath. */
    public static final String ZOMBIE_SPRITE = "/images/zombie.png";
}
